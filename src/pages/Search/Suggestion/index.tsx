import type React from 'react'
import { memo } from 'react'

interface Props {}

const Suggestion: React.FC<Props> = memo(() => {
  return
})
Suggestion.displayName = 'Suggestion'

export default Suggestion
